# Autogenerated by Sphinx on Tue Aug 12 15:11:12 2025
# as part of the release process.

library = {
    "__future__": 'Imports of the form "from __future__ import feature" are called future\nstatements. These are special-cased by the Python compiler to allow\nthe use of new Python features in modules containing the future\nstatement before the release in which the feature becomes standard.',
    "__main__": 'In Python, the special name "__main__" is used for two important\nconstructs:',
    "_thread": 'This module provides low-level primitives for working with multiple\nthreads (also called *light-weight processes* or *tasks*) \u2014 multiple\nthreads of control sharing their global data space.  For\nsynchronization, simple locks (also called *mutexes* or *binary\nsemaphores*) are provided. The "threading" module provides an easier\nto use and higher-level threading API built on top of this module.',
    "abc": 'This module provides the infrastructure for defining *abstract base\nclasses* (ABCs) in Python, as outlined in **PEP 3119**; see the PEP\nfor why this was added to Python. (See also **PEP 3141** and the\n"numbers" module regarding a type hierarchy for numbers based on\nABCs.)',
    "aifc": 'This module is no longer part of the Python standard library. It was\nremoved in Python 3.13 after being deprecated in Python 3.11.  The\nremoval was decided in **PEP 594**.\n\nThe last version of Python that provided the "aifc" module was Python\n3.12.',
    "annotationlib": 'The "annotationlib" module provides tools for introspecting\n*annotations* on modules, classes, and functions.',
    "argparse": 'While "argparse" is the default recommended standard library module\nfor implementing basic command line applications, authors with more\nexacting requirements for exactly how their command line applications\nbehave may find it doesn\u2019t provide the necessary level of control.\nRefer to Choosing an argument parsing library for alternatives to\nconsider when "argparse" doesn\u2019t support behaviors that the\napplication requires (such as entirely disabling support for\ninterspersed options and positional arguments, or accepting option\nparameter values that start with "-" even when they correspond to\nanother defined option).',
    "array": "This module defines an object type which can compactly represent an\narray of basic values: characters, integers, floating-point numbers.\nArrays are sequence types and behave very much like lists, except that\nthe type of objects stored in them is constrained.  The type is\nspecified at object creation time by using a *type code*, which is a\nsingle character.  The following type codes are defined:",
    "ast": 'The "ast" module helps Python applications to process trees of the\nPython abstract syntax grammar.  The abstract syntax itself might\nchange with each Python release; this module helps to find out\nprogrammatically what the current grammar looks like.',
    "asynchat": 'This module is no longer part of the Python standard library. It was\nremoved in Python 3.12 after being deprecated in Python 3.6.  The\nremoval was decided in **PEP 594**.\n\nApplications should use the "asyncio" module instead.\n\nThe last version of Python that provided the "asynchat" module was\nPython 3.11.',
    "asyncio": "asyncio is a library to write **concurrent** code using the\n**async/await** syntax.",
    "asyncio-api-index": "This page lists all high-level async/await enabled asyncio APIs.",
    "asyncio-dev": "Asynchronous programming is different from classic \u201csequential\u201d\nprogramming.",
    "asyncio-eventloop": "The event loop is the core of every asyncio application. Event loops\nrun asynchronous tasks and callbacks, perform network IO operations,\nand run subprocesses.",
    "asyncio-exceptions": 'A deprecated alias of "TimeoutError", raised when the operation has\nexceeded the given deadline.',
    "asyncio-extending": 'The main direction for "asyncio" extending is writing custom *event\nloop* classes. Asyncio has helpers that could be used to simplify this\ntask.',
    "asyncio-future": "*Future* objects are used to bridge **low-level callback-based code**\nwith high-level async/await code.",
    "asyncio-graph": "asyncio has powerful runtime call graph introspection utilities to\ntrace the entire call graph of a running *coroutine* or *task*, or a\nsuspended *future*.  These utilities and the underlying machinery can\nbe used from within a Python program or by external profilers and\ndebuggers.",
    "asyncio-llapi-index": "This page lists all low-level asyncio APIs.",
    "asyncio-platforms": 'The "asyncio" module is designed to be portable, but some platforms\nhave subtle differences and limitations due to the platforms\u2019\nunderlying architecture and capabilities.',
    "asyncio-policy": 'Policies are deprecated and will be removed in Python 3.16. Users are\nencouraged to use the "asyncio.run()" function or the "asyncio.Runner"\nwith *loop_factory* to use the desired loop implementation.',
    "asyncio-protocol": 'Transports and Protocols are used by the **low-level** event loop APIs\nsuch as "loop.create_connection()".  They use callback-based\nprogramming style and enable high-performance implementations of\nnetwork or IPC protocols (e.g. HTTP).',
    "asyncio-queue": 'asyncio queues are designed to be similar to classes of the "queue"\nmodule.  Although asyncio queues are not thread-safe, they are\ndesigned to be used specifically in async/await code.',
    "asyncio-runner": "This section outlines high-level asyncio primitives to run asyncio\ncode.",
    "asyncio-stream": "Streams are high-level async/await-ready primitives to work with\nnetwork connections.  Streams allow sending and receiving data without\nusing callbacks or low-level protocols and transports.",
    "asyncio-subprocess": "This section describes high-level async/await asyncio APIs to create\nand manage subprocesses.",
    "asyncio-sync": 'asyncio synchronization primitives are designed to be similar to those\nof the "threading" module with two important caveats:',
    "asyncio-task": "This section outlines high-level asyncio APIs to work with coroutines\nand Tasks.",
    "asyncore": 'This module is no longer part of the Python standard library. It was\nremoved in Python 3.12 after being deprecated in Python 3.6.  The\nremoval was decided in **PEP 594**.\n\nApplications should use the "asyncio" module instead.\n\nThe last version of Python that provided the "asyncore" module was\nPython 3.11.',
    "atexit": 'The "atexit" module defines functions to register and unregister\ncleanup functions.  Functions thus registered are automatically\nexecuted upon normal interpreter termination.  "atexit" runs these\nfunctions in the *reverse* order in which they were registered; if you\nregister "A", "B", and "C", at interpreter termination time they will\nbe run in the order "C", "B", "A".',
    "audioop": 'This module is no longer part of the Python standard library. It was\nremoved in Python 3.13 after being deprecated in Python 3.11.  The\nremoval was decided in **PEP 594**.\n\nThe last version of Python that provided the "audioop" module was\nPython 3.12.',
    "audit_events": 'This table contains all events raised by "sys.audit()" or\n"PySys_Audit()" calls throughout the CPython runtime and the standard\nlibrary.  These calls were added in 3.8 or later (see **PEP 578**).',
    "base64": "This module provides functions for encoding binary data to printable\nASCII characters and decoding such encodings back to binary data. This\nincludes the encodings specified in **RFC 4648** (Base64, Base32 and\nBase16) and the non-standard Base85 encodings.",
    "bdb": 'The "bdb" module handles basic debugger functions, like setting\nbreakpoints or managing execution via the debugger.',
    "binascii": 'The "binascii" module contains a number of methods to convert between\nbinary and various ASCII-encoded binary representations. Normally, you\nwill not use these functions directly but use wrapper modules like\n"base64" instead. The "binascii" module contains low-level functions\nwritten in C for greater speed that are used by the higher-level\nmodules.',
    "bisect": "This module provides support for maintaining a list in sorted order\nwithout having to sort the list after each insertion.  For long lists\nof items with expensive comparison operations, this can be an\nimprovement over linear searches or frequent resorting.",
    "builtins": 'This module provides direct access to all \u2018built-in\u2019 identifiers of\nPython; for example, "builtins.open" is the full name for the built-in\nfunction "open()".',
    "bz2": "This module provides a comprehensive interface for compressing and\ndecompressing data using the bzip2 compression algorithm.",
    "calendar": 'This module allows you to output calendars like the Unix **cal**\nprogram, and provides additional useful functions related to the\ncalendar. By default, these calendars have Monday as the first day of\nthe week, and Sunday as the last (the European convention). Use\n"setfirstweekday()" to set the first day of the week to Sunday (6) or\nto any other weekday.  Parameters that specify dates are given as\nintegers. For related functionality, see also the "datetime" and\n"time" modules.',
    "cgi": 'This module is no longer part of the Python standard library. It was\nremoved in Python 3.13 after being deprecated in Python 3.11.  The\nremoval was decided in **PEP 594**.\n\nA fork of the module on PyPI can be used instead: legacy-cgi. This is\na copy of the cgi module, no longer maintained or supported by the\ncore Python team.\n\nThe last version of Python that provided the "cgi" module was Python\n3.12.',
    "cgitb": 'This module is no longer part of the Python standard library. It was\nremoved in Python 3.13 after being deprecated in Python 3.11.  The\nremoval was decided in **PEP 594**.\n\nA fork of the module on PyPI can now be used instead: legacy-cgi. This\nis a copy of the cgi module, no longer maintained or supported by the\ncore Python team.\n\nThe last version of Python that provided the "cgitb" module was Python\n3.12.',
    "chunk": 'This module is no longer part of the Python standard library. It was\nremoved in Python 3.13 after being deprecated in Python 3.11.  The\nremoval was decided in **PEP 594**.\n\nThe last version of Python that provided the "chunk" module was Python\n3.12.',
    "cmath": 'This module provides access to mathematical functions for complex\nnumbers.  The functions in this module accept integers, floating-point\nnumbers or complex numbers as arguments. They will also accept any\nPython object that has either a "__complex__()" or a "__float__()"\nmethod: these methods are used to convert the object to a complex or\nfloating-point number, respectively, and the function is then applied\nto the result of the conversion.',
    "cmd": 'The "Cmd" class provides a simple framework for writing line-oriented\ncommand interpreters.  These are often useful for test harnesses,\nadministrative tools, and prototypes that will later be wrapped in a\nmore sophisticated interface.',
    "cmdline": "The following modules have a command-line interface.",
    "code": 'The "code" module provides facilities to implement read-eval-print\nloops in Python.  Two classes and convenience functions are included\nwhich can be used to build applications which provide an interactive\ninterpreter prompt.',
    "codecs": 'This module defines base classes for standard Python codecs (encoders\nand decoders) and provides access to the internal Python codec\nregistry, which manages the codec and error handling lookup process.\nMost standard codecs are *text encodings*, which encode text to bytes\n(and decode bytes to text), but there are also codecs provided that\nencode text to text, and bytes to bytes. Custom codecs may encode and\ndecode between arbitrary types, but some module features are\nrestricted to be used specifically with *text encodings* or with\ncodecs that encode to "bytes".',
    "codeop": 'The "codeop" module provides utilities upon which the Python read-\neval-print loop can be emulated, as is done in the "code" module.  As\na result, you probably don\u2019t want to use the module directly; if you\nwant to include such a loop in your program you probably want to use\nthe "code" module instead.',
    "collections": 'This module implements specialized container datatypes providing\nalternatives to Python\u2019s general purpose built-in containers, "dict",\n"list", "set", and "tuple".',
    "collections.abc": "This module provides *abstract base classes* that can be used to test\nwhether a class provides a particular interface; for example, whether\nit is *hashable* or whether it is a *mapping*.",
    "colorsys": 'The "colorsys" module defines bidirectional conversions of color\nvalues between colors expressed in the RGB (Red Green Blue) color\nspace used in computer monitors and three other coordinate systems:\nYIQ, HLS (Hue Lightness Saturation) and HSV (Hue Saturation Value).\nCoordinates in all of these color spaces are floating-point values.\nIn the YIQ space, the Y coordinate is between 0 and 1, but the I and Q\ncoordinates can be positive or negative.  In all other spaces, the\ncoordinates are all between 0 and 1.',
    "compileall": "This module provides some utility functions to support installing\nPython libraries.  These functions compile Python source files in a\ndirectory tree. This module can be used to create the cached byte-code\nfiles at library installation time, which makes them available for use\neven by users who don\u2019t have write permission to the library\ndirectories.",
    "compression": 'The "compression" package contains the canonical compression modules\ncontaining interfaces to several different compression algorithms.\nSome of these modules have historically been available as separate\nmodules; those will continue to be available under their original\nnames for compatibility reasons, and will not be removed without a\ndeprecation cycle. The use of modules in "compression" is encouraged\nwhere practical.',
    "compression.zstd": 'This module provides classes and functions for compressing and\ndecompressing data using the Zstandard (or *zstd*) compression\nalgorithm. The zstd manual describes Zstandard as \u201ca fast lossless\ncompression algorithm, targeting real-time compression scenarios at\nzlib-level and better compression ratios.\u201d Also included is a file\ninterface that supports reading and writing the contents of ".zst"\nfiles created by the **zstd** utility, as well as raw zstd compressed\nstreams.',
    "concurrent": "This package contains the following modules:",
    "concurrent.futures": 'The "concurrent.futures" module provides a high-level interface for\nasynchronously executing callables.',
    "concurrent.interpreters": 'The "concurrent.interpreters" module constructs higher-level\ninterfaces on top of the lower level "_interpreters" module.',
    "configparser": 'This module provides the "ConfigParser" class which implements a basic\nconfiguration language which provides a structure similar to what\u2019s\nfound in Microsoft Windows INI files.  You can use this to write\nPython programs which can be customized by end users easily.',
    "constants": "A small number of constants live in the built-in namespace.  They are:",
    "contextlib": 'This module provides utilities for common tasks involving the "with"\nstatement. For more information see also Context Manager Types and\nWith Statement Context Managers.',
    "contextvars": 'This module provides APIs to manage, store, and access context-local\nstate.  The "ContextVar" class is used to declare and work with\n*Context Variables*.  The "copy_context()" function and the "Context"\nclass should be used to manage the current context in asynchronous\nframeworks.',
    "copy": "Assignment statements in Python do not copy objects, they create\nbindings between a target and an object. For collections that are\nmutable or contain mutable items, a copy is sometimes needed so one\ncan change one copy without changing the other. This module provides\ngeneric shallow and deep copy operations (explained below).",
    "copyreg": 'The "copyreg" module offers a way to define functions used while\npickling specific objects.  The "pickle" and "copy" modules use those\nfunctions when pickling/copying those objects.  The module provides\nconfiguration information about object constructors which are not\nclasses. Such constructors may be factory functions or class\ninstances.',
    "crypt": 'This module is no longer part of the Python standard library. It was\nremoved in Python 3.13 after being deprecated in Python 3.11.  The\nremoval was decided in **PEP 594**.\n\nApplications can use the "hashlib" module from the standard library.\nOther possible replacements are third-party libraries from PyPI:\nlegacycrypt, bcrypt, argon2-cffi, or passlib. These are not supported\nor maintained by the Python core team.\n\nThe last version of Python that provided the "crypt" module was Python\n3.12.',
    "csv": "The so-called CSV (Comma Separated Values) format is the most common\nimport and export format for spreadsheets and databases.  CSV format\nwas used for many years prior to attempts to describe the format in a\nstandardized way in **RFC 4180**.  The lack of a well-defined standard\nmeans that subtle differences often exist in the data produced and\nconsumed by different applications.  These differences can make it\nannoying to process CSV files from multiple sources. Still, while the\ndelimiters and quoting characters vary, the overall format is similar\nenough that it is possible to write a single module which can\nefficiently manipulate such data, hiding the details of reading and\nwriting the data from the programmer.",
    "ctypes": '"ctypes" is a foreign function library for Python.  It provides C\ncompatible data types, and allows calling functions in DLLs or shared\nlibraries.  It can be used to wrap these libraries in pure Python.',
    "curses": 'The "curses" module provides an interface to the curses library, the\nde-facto standard for portable advanced terminal handling.',
    "curses.ascii": 'The "curses.ascii" module supplies name constants for ASCII characters\nand functions to test membership in various ASCII character classes.\nThe constants supplied are names for control characters as follows:',
    "curses.panel": "Panels are windows with the added feature of depth, so they can be\nstacked on top of each other, and only the visible portions of each\nwindow will be displayed.  Panels can be added, moved up or down in\nthe stack, and removed.",
    "dataclasses": 'This module provides a decorator and functions for automatically\nadding generated *special methods* such as "__init__()" and\n"__repr__()" to user-defined classes.  It was originally described in\n**PEP 557**.',
    "datetime": 'The "datetime" module supplies classes for manipulating dates and\ntimes.',
    "dbm": '"dbm" is a generic interface to variants of the DBM database:',
    "debug": "These libraries help you with Python development: the debugger enables\nyou to step through code, analyze stack frames and set breakpoints\netc., and the profilers run code and give you a detailed breakdown of\nexecution times, allowing you to identify bottlenecks in your\nprograms. Auditing events provide visibility into runtime behaviors\nthat would otherwise require intrusive debugging or patching.",
    "decimal": 'The "decimal" module provides support for fast correctly rounded\ndecimal floating-point arithmetic. It offers several advantages over\nthe "float" datatype:',
    "devmode": "The Python Development Mode introduces additional runtime checks that\nare too expensive to be enabled by default. It should not be more\nverbose than the default if the code is correct; new warnings are only\nemitted when an issue is detected.",
    "dialog": 'The "tkinter.simpledialog" module contains convenience classes and\nfunctions for creating simple modal dialogs to get a value from the\nuser.',
    "difflib": 'This module provides classes and functions for comparing sequences. It\ncan be used for example, for comparing files, and can produce\ninformation about file differences in various formats, including HTML\nand context and unified diffs. For comparing directories and files,\nsee also, the "filecmp" module.',
    "dis": 'The "dis" module supports the analysis of CPython *bytecode* by\ndisassembling it. The CPython bytecode which this module takes as an\ninput is defined in the file "Include/opcode.h" and used by the\ncompiler and the interpreter.',
    "distribution": "These libraries help you with publishing and installing Python\nsoftware. While these modules are designed to work in conjunction with\nthe Python Package Index, they can also be used with a local index\nserver, or without any index server at all.",
    "distutils": 'This module is no longer part of the Python standard library. It was\nremoved in Python 3.12 after being deprecated in Python 3.10.  The\nremoval was decided in **PEP 632**, which has migration advice.\n\nThe last version of Python that provided the "distutils" module was\nPython 3.11.',
    "doctest": 'The "doctest" module searches for pieces of text that look like\ninteractive Python sessions, and then executes those sessions to\nverify that they work exactly as shown.  There are several common ways\nto use doctest:',
    "email": 'The "email" package is a library for managing email messages.  It is\nspecifically *not* designed to do any sending of email messages to\nSMTP (**RFC 2821**), NNTP, or other servers; those are functions of\nmodules such as "smtplib".  The "email" package attempts to be as RFC-\ncompliant as possible, supporting **RFC 5322** and **RFC 6532**, as\nwell as such MIME-related RFCs as **RFC 2045**, **RFC 2046**, **RFC\n2047**, **RFC 2183**, and **RFC 2231**.',
    "email.charset": 'This module is part of the legacy ("Compat32") email API.  In the new\nAPI only the aliases table is used.',
    "email.compat32-message": 'The "Message" class is very similar to the "EmailMessage" class,\nwithout the methods added by that class, and with the default behavior\nof certain other methods being slightly different.  We also document\nhere some methods that, while supported by the "EmailMessage" class,\nare not recommended unless you are dealing with legacy code.',
    "email.contentmanager": 'Base class for content managers.  Provides the standard registry\nmechanisms to register converters between MIME content and other\nrepresentations, as well as the "get_content" and "set_content"\ndispatch methods.',
    "email.encoders": 'This module is part of the legacy ("Compat32") email API.  In the new\nAPI the functionality is provided by the *cte* parameter of the\n"set_content()" method.',
    "email.errors": 'The following exception classes are defined in the "email.errors"\nmodule:',
    "email.examples": 'Here are a few examples of how to use the "email" package to read,\nwrite, and send simple email messages, as well as more complex MIME\nmessages.',
    "email.generator": 'One of the most common tasks is to generate the flat (serialized)\nversion of the email message represented by a message object\nstructure.  You will need to do this if you want to send your message\nvia "smtplib.SMTP.sendmail()", or print the message on the console.\nTaking a message object structure and producing a serialized\nrepresentation is the job of the generator classes.',
    "email.header": 'This module is part of the legacy ("Compat32") email API.  In the\ncurrent API encoding and decoding of headers is handled transparently\nby the dictionary-like API of the "EmailMessage" class.  In addition\nto uses in legacy code, this module can be useful in applications that\nneed to completely control the character sets used when encoding\nheaders.',
    "email.headerregistry": 'Headers are represented by customized subclasses of "str".  The\nparticular class used to represent a given header is determined by the\n"header_factory" of the "policy" in effect when the headers are\ncreated.  This section documents the particular "header_factory"\nimplemented by the email package for handling **RFC 5322** compliant\nemail messages, which not only provides customized header objects for\nvarious header types, but also provides an extension mechanism for\napplications to add their own custom header types.',
    "email.iterators": 'Iterating over a message object tree is fairly easy with the\n"Message.walk" method.  The "email.iterators" module provides some\nuseful higher level iterations over message object trees.',
    "email.message": 'The central class in the "email" package is the "EmailMessage" class,\nimported from the "email.message" module.  It is the base class for\nthe "email" object model.  "EmailMessage" provides the core\nfunctionality for setting and querying header fields, for accessing\nmessage bodies, and for creating or modifying structured messages.',
    "email.mime": 'This module is part of the legacy ("Compat32") email API.  Its\nfunctionality is partially replaced by the "contentmanager" in the new\nAPI, but in certain applications these classes may still be useful,\neven in non-legacy code.',
    "email.parser": 'Message object structures can be created in one of two ways: they can\nbe created from whole cloth by creating an "EmailMessage" object,\nadding headers using the dictionary interface, and adding payload(s)\nusing "set_content()" and related methods, or they can be created by\nparsing a serialized representation of the email message.',
    "email.policy": 'The "email" package\u2019s prime focus is the handling of email messages as\ndescribed by the various email and MIME RFCs.  However, the general\nformat of email messages (a block of header fields each consisting of\na name followed by a colon followed by a value, the whole block\nfollowed by a blank line and an arbitrary \u2018body\u2019), is a format that\nhas found utility outside of the realm of email.  Some of these uses\nconform fairly closely to the main email RFCs, some do not.  Even when\nworking with email, there are times when it is desirable to break\nstrict compliance with the RFCs, such as generating emails that\ninteroperate with email servers that do not themselves follow the\nstandards, or that implement extensions you want to use in ways that\nviolate the standards.',
    "email.utils": 'There are a couple of useful utilities provided in the "email.utils"\nmodule:',
    "ensurepip": 'The "ensurepip" package provides support for bootstrapping the "pip"\ninstaller into an existing Python installation or virtual environment.\nThis bootstrapping approach reflects the fact that "pip" is an\nindependent project with its own release cycle, and the latest\navailable stable version is bundled with maintenance and feature\nreleases of the CPython reference interpreter.',
    "enum": "This page contains the API reference information. For tutorial\ninformation and discussion of more advanced topics, see",
    "errno": 'This module makes available standard "errno" system symbols. The value\nof each symbol is the corresponding integer value. The names and\ndescriptions are borrowed from "linux/include/errno.h", which should\nbe all-inclusive.',
    "exceptions": 'In Python, all exceptions must be instances of a class that derives\nfrom "BaseException".  In a "try" statement with an "except" clause\nthat mentions a particular class, that clause also handles any\nexception classes derived from that class (but not exception classes\nfrom which *it* is derived).  Two exception classes that are not\nrelated via subclassing are never equivalent, even if they have the\nsame name.',
    "faulthandler": 'This module contains functions to dump Python tracebacks explicitly,\non a fault, after a timeout, or on a user signal. Call\n"faulthandler.enable()" to install fault handlers for the "SIGSEGV",\n"SIGFPE", "SIGABRT", "SIGBUS", and "SIGILL" signals. You can also\nenable them at startup by setting the "PYTHONFAULTHANDLER" environment\nvariable or by using the "-X" "faulthandler" command line option.',
    "fcntl": 'This module performs file and I/O control on file descriptors. It is\nan interface to the "fcntl()" and "ioctl()" Unix routines. See the\n*fcntl(2)* and *ioctl(2)* Unix manual pages for full details.',
    "filecmp": 'The "filecmp" module defines functions to compare files and\ndirectories, with various optional time/correctness trade-offs. For\ncomparing files, see also the "difflib" module.',
    "fileinput": 'This module implements a helper class and functions to quickly write a\nloop over standard input or a list of files. If you just want to read\nor write one file see "open()".',
    "fnmatch": 'This module provides support for Unix shell-style wildcards, which are\n*not* the same as regular expressions (which are documented in the\n"re" module).  The special characters used in shell-style wildcards\nare:',
    "fractions": 'The "fractions" module provides support for rational number\narithmetic.',
    "ftplib": 'This module defines the class "FTP" and a few related items. The "FTP"\nclass implements the client side of the FTP protocol.  You can use\nthis to write Python programs that perform a variety of automated FTP\njobs, such as mirroring other FTP servers.  It is also used by the\nmodule "urllib.request" to handle URLs that use FTP.  For more\ninformation on FTP (File Transfer Protocol), see internet **RFC 959**.',
    "functions": "The Python interpreter has a number of functions and types built into\nit that are always available.  They are listed here in alphabetical\norder.",
    "functools": 'The "functools" module is for higher-order functions: functions that\nact on or return other functions. In general, any callable object can\nbe treated as a function for the purposes of this module.',
    "gc": 'This module provides an interface to the optional garbage collector.\nIt provides the ability to disable the collector, tune the collection\nfrequency, and set debugging options.  It also provides access to\nunreachable objects that the collector found but cannot free.  Since\nthe collector supplements the reference counting already used in\nPython, you can disable the collector if you are sure your program\ndoes not create reference cycles.  Automatic collection can be\ndisabled by calling "gc.disable()".  To debug a leaking program call\n"gc.set_debug(gc.DEBUG_LEAK)". Notice that this includes\n"gc.DEBUG_SAVEALL", causing garbage-collected objects to be saved in\ngc.garbage for inspection.',
    "getopt": 'This module is considered feature complete. A more declarative and\nextensible alternative to this API is provided in the "optparse"\nmodule. Further functional enhancements for command line parameter\nprocessing are provided either as third party modules on PyPI, or else\nas features in the "argparse" module.',
    "getpass": "Availability: not WASI.",
    "gettext": 'The "gettext" module provides internationalization (I18N) and\nlocalization (L10N) services for your Python modules and applications.\nIt supports both the GNU **gettext** message catalog API and a higher\nlevel, class-based API that may be more appropriate for Python files.\nThe interface described below allows you to write your module and\napplication messages in one natural language, and provide a catalog of\ntranslated messages for running under different natural languages.',
    "glob": 'The "glob" module finds all the pathnames matching a specified pattern\naccording to the rules used by the Unix shell, although results are\nreturned in arbitrary order.  No tilde expansion is done, but "*",\n"?", and character ranges expressed with "[]" will be correctly\nmatched.  This is done by using the "os.scandir()" and\n"fnmatch.fnmatch()" functions in concert, and not by actually invoking\na subshell.',
    "graphlib": "Provides functionality to topologically sort a graph of *hashable*\nnodes.",
    "grp": "This module provides access to the Unix group database. It is\navailable on all Unix versions.",
    "gzip": "This module provides a simple interface to compress and decompress\nfiles just like the GNU programs **gzip** and **gunzip** would.",
    "hashlib": "This module implements a common interface to many different hash\nalgorithms. Included are the FIPS secure hash algorithms SHA224,\nSHA256, SHA384, SHA512, (defined in the FIPS 180-4 standard), the\nSHA-3 series (defined in the FIPS 202 standard) as well as the legacy\nalgorithms SHA1 (formerly part of FIPS) and the MD5 algorithm (defined\nin internet **RFC 1321**).",
    "heapq": "This module provides an implementation of the heap queue algorithm,\nalso known as the priority queue algorithm.",
    "hmac": "This module implements the HMAC algorithm as described by **RFC\n2104**. The interface allows to use any hash function with a *fixed*\ndigest size. In particular, extendable output functions such as\nSHAKE-128 or SHAKE-256 cannot be used with HMAC.",
    "html": "This module defines utilities to manipulate HTML.",
    "html.entities": 'This module defines four dictionaries, "html5", "name2codepoint",\n"codepoint2name", and "entitydefs".',
    "html.parser": 'This module defines a class "HTMLParser" which serves as the basis for\nparsing text files formatted in HTML (HyperText Mark-up Language) and\nXHTML.',
    "http": '"http" is a package that collects several modules for working with the\nHyperText Transfer Protocol:',
    "http.client": 'This module defines classes that implement the client side of the HTTP\nand HTTPS protocols.  It is normally not used directly \u2014 the module\n"urllib.request" uses it to handle URLs that use HTTP and HTTPS.',
    "http.cookiejar": 'The "http.cookiejar" module defines classes for automatic handling of\nHTTP cookies.  It is useful for accessing web sites that require small\npieces of data \u2013 *cookies* \u2013 to be set on the client machine by an\nHTTP response from a web server, and then returned to the server in\nlater HTTP requests.',
    "http.cookies": 'The "http.cookies" module defines classes for abstracting the concept\nof cookies, an HTTP state management mechanism. It supports both\nsimple string-only cookies, and provides an abstraction for having any\nserializable data-type as cookie value.',
    "http.server": "This module defines classes for implementing HTTP servers.",
    "idle": "IDLE is Python\u2019s Integrated Development and Learning Environment.",
    "imaplib": 'This module defines three classes, "IMAP4", "IMAP4_SSL" and\n"IMAP4_stream", which encapsulate a connection to an IMAP4 server and\nimplement a large subset of the IMAP4rev1 client protocol as defined\nin **RFC 2060**. It is backward compatible with IMAP4 (**RFC 1730**)\nservers, but note that the "STATUS" command is not supported in IMAP4.',
    "imghdr": 'This module is no longer part of the Python standard library. It was\nremoved in Python 3.13 after being deprecated in Python 3.11.  The\nremoval was decided in **PEP 594**.\n\nPossible replacements are third-party libraries from PyPI: filetype,\npuremagic, or python-magic. These are not supported or maintained by\nthe Python core team.\n\nThe last version of Python that provided the "imghdr" module was\nPython 3.12.',
    "imp": 'This module is no longer part of the Python standard library. It was\nremoved in Python 3.12 after being deprecated in Python 3.4.\n\nThe removal notice includes guidance for migrating code from "imp" to\n"importlib".\n\nThe last version of Python that provided the "imp" module was Python\n3.11.',
    "importlib": 'The purpose of the "importlib" package is three-fold.',
    "importlib.metadata": 'Changed in version 3.10: "importlib.metadata" is no longer\nprovisional.',
    "importlib.resources": "This module leverages Python\u2019s import system to provide access to\n*resources* within *packages*.",
    "importlib.resources.abc": "*Superseded by TraversableResources*",
    "index": "While The Python Language Reference describes the exact syntax and\nsemantics of the Python language, this library reference manual\ndescribes the standard library that is distributed with Python. It\nalso describes some of the optional components that are commonly\nincluded in Python distributions.",
    "inspect": 'The "inspect" module provides several useful functions to help get\ninformation about live objects such as modules, classes, methods,\nfunctions, tracebacks, frame objects, and code objects.  For example,\nit can help you examine the contents of a class, retrieve the source\ncode of a method, extract and format the argument list for a function,\nor get all the information you need to display a detailed traceback.',
    "intro": "The \u201cPython library\u201d contains several different kinds of components.",
    "io": 'The "io" module provides Python\u2019s main facilities for dealing with\nvarious types of I/O.  There are three main types of I/O: *text I/O*,\n*binary I/O* and *raw I/O*.  These are generic categories, and various\nbacking stores can be used for each of them.  A concrete object\nbelonging to any of these categories is called a *file object*.  Other\ncommon terms are *stream* and *file-like object*.',
    "ipaddress": '"ipaddress" provides the capabilities to create, manipulate and\noperate on IPv4 and IPv6 addresses and networks.',
    "itertools": "This module implements a number of *iterator* building blocks inspired\nby constructs from APL, Haskell, and SML.  Each has been recast in a\nform suitable for Python.",
    "json": "JSON (JavaScript Object Notation), specified by **RFC 7159** (which\nobsoletes **RFC 4627**) and by ECMA-404, is a lightweight data\ninterchange format inspired by JavaScript object literal syntax\n(although it is not a strict subset of JavaScript [1] ).",
    "keyword": "This module allows a Python program to determine if a string is a\nkeyword or soft keyword.",
    "language": "Python provides a number of modules to assist in working with the\nPython language.  These modules support tokenizing, parsing, syntax\nanalysis, bytecode disassembly, and various other facilities.",
    "linecache": 'The "linecache" module allows one to get any line from a Python source\nfile, while attempting to optimize internally, using a cache, the\ncommon case where many lines are read from a single file.  This is\nused by the "traceback" module to retrieve source lines for inclusion\nin  the formatted traceback.',
    "locale": 'The "locale" module opens access to the POSIX locale database and\nfunctionality. The POSIX locale mechanism allows programmers to deal\nwith certain cultural issues in an application, without requiring the\nprogrammer to know all the specifics of each country where the\nsoftware is executed.',
    "logging": "This page contains the API reference information. For tutorial\ninformation and discussion of more advanced topics, see",
    "logging.config": "This page contains only reference information. For tutorials, please\nsee",
    "logging.handlers": "This page contains only reference information. For tutorials, please\nsee",
    "lzma": 'This module provides classes and convenience functions for compressing\nand decompressing data using the LZMA compression algorithm. Also\nincluded is a file interface supporting the ".xz" and legacy ".lzma"\nfile formats used by the **xz** utility, as well as raw compressed\nstreams.',
    "mailbox": 'This module defines two classes, "Mailbox" and "Message", for\naccessing and manipulating on-disk mailboxes and the messages they\ncontain. "Mailbox" offers a dictionary-like mapping from keys to\nmessages. "Message" extends the "email.message" module\u2019s "Message"\nclass with format-specific state and behavior. Supported mailbox\nformats are Maildir, mbox, MH, Babyl, and MMDF.',
    "mailcap": 'This module is no longer part of the Python standard library. It was\nremoved in Python 3.13 after being deprecated in Python 3.11.  The\nremoval was decided in **PEP 594**.\n\nThe last version of Python that provided the "mailcap" module was\nPython 3.12.',
    "markup": "Python supports a variety of modules to work with various forms of\nstructured data markup.  This includes modules to work with the\nStandard Generalized Markup Language (SGML) and the Hypertext Markup\nLanguage (HTML), and several interfaces for working with the\nExtensible Markup Language (XML).",
    "marshal": "This module contains functions that can read and write Python values\nin a binary format.  The format is specific to Python, but independent\nof machine architecture issues (e.g., you can write a Python value to\na file on a PC, transport the file to a Mac, and read it back there).\nDetails of the format are undocumented on purpose; it may change\nbetween Python versions (although it rarely does). [1]",
    "math": "This module provides access to common mathematical functions and\nconstants, including those defined by the C standard.",
    "mimetypes": 'The "mimetypes" module converts between a filename or URL and the MIME\ntype associated with the filename extension.  Conversions are provided\nfrom filename to MIME type and from MIME type to filename extension;\nencodings are not supported for the latter conversion.',
    "mmap": "Availability: not WASI.",
    "modulefinder": 'This module provides a "ModuleFinder" class that can be used to\ndetermine the set of modules imported by a script. "modulefinder.py"\ncan also be run as a script, giving the filename of a Python script as\nits argument, after which a report of the imported modules will be\nprinted.',
    "msilib": 'This module is no longer part of the Python standard library. It was\nremoved in Python 3.13 after being deprecated in Python 3.11.  The\nremoval was decided in **PEP 594**.\n\nThe last version of Python that provided the "msilib" module was\nPython 3.12.',
    "msvcrt": 'These functions provide access to some useful capabilities on Windows\nplatforms. Some higher-level modules use these functions to build the\nWindows implementations of their services. For example, the "getpass"\nmodule uses this in the implementation of the "getpass()" function.',
    "multiprocessing": "Availability: not Android, not iOS, not WASI.",
    "multiprocessing.shared_memory": 'This module provides a class, "SharedMemory", for the allocation and\nmanagement of shared memory to be accessed by one or more processes on\na multicore or symmetric multiprocessor (SMP) machine.  To assist with\nthe life-cycle management of shared memory especially across distinct\nprocesses, a "BaseManager" subclass, "SharedMemoryManager", is also\nprovided in the "multiprocessing.managers" module.',
    "netdata": "This chapter describes modules which support handling data formats\ncommonly used on the internet.",
    "netrc": 'The "netrc" class parses and encapsulates the netrc file format used\nby the Unix **ftp** program and other FTP clients.',
    "nis": 'This module is no longer part of the Python standard library. It was\nremoved in Python 3.13 after being deprecated in Python 3.11.  The\nremoval was decided in **PEP 594**.\n\nThe last version of Python that provided the "nis" module was Python\n3.12.',
    "nntplib": 'This module is no longer part of the Python standard library. It was\nremoved in Python 3.13 after being deprecated in Python 3.11.  The\nremoval was decided in **PEP 594**.\n\nThe last version of Python that provided the "nntplib" module was\nPython 3.12.',
    "numbers": 'The "numbers" module (**PEP 3141**) defines a hierarchy of numeric\n*abstract base classes* which progressively define more operations.\nNone of the types defined in this module are intended to be\ninstantiated.',
    "operator": 'The "operator" module exports a set of efficient functions\ncorresponding to the intrinsic operators of Python.  For example,\n"operator.add(x, y)" is equivalent to the expression "x+y". Many\nfunction names are those used for special methods, without the double\nunderscores.  For backward compatibility, many of these have a variant\nwith the double underscores kept. The variants without the double\nunderscores are preferred for clarity.',
    "optparse": "The standard library includes three argument parsing libraries:",
    "os": 'This module provides a portable way of using operating system\ndependent functionality.  If you just want to read or write a file see\n"open()", if you want to manipulate paths, see the "os.path" module,\nand if you want to read all the lines in all the files on the command\nline see the "fileinput" module.  For creating temporary files and\ndirectories see the "tempfile" module, and for high-level file and\ndirectory handling see the "shutil" module.',
    "os.path": 'This module implements some useful functions on pathnames. To read or\nwrite files see "open()", and for accessing the filesystem see the\n"os" module. The path parameters can be passed as strings, or bytes,\nor any object implementing the "os.PathLike" protocol.',
    "ossaudiodev": 'This module is no longer part of the Python standard library. It was\nremoved in Python 3.13 after being deprecated in Python 3.11.  The\nremoval was decided in **PEP 594**.\n\nThe last version of Python that provided the "ossaudiodev" module was\nPython 3.12.',
    "pathlib": "This module offers classes representing filesystem paths with\nsemantics appropriate for different operating systems.  Path classes\nare divided between pure paths, which provide purely computational\noperations without I/O, and concrete paths, which inherit from pure\npaths but also provide I/O operations.",
    "pdb": 'The module "pdb" defines an interactive source code debugger for\nPython programs.  It supports setting (conditional) breakpoints and\nsingle stepping at the source line level, inspection of stack frames,\nsource code listing, and evaluation of arbitrary Python code in the\ncontext of any stack frame.  It also supports post-mortem debugging\nand can be called under program control.',
    "pickle": 'The "pickle" module implements binary protocols for serializing and\nde-serializing a Python object structure.  *\u201cPickling\u201d* is the process\nwhereby a Python object hierarchy is converted into a byte stream, and\n*\u201cunpickling\u201d* is the inverse operation, whereby a byte stream (from a\n*binary file* or *bytes-like object*) is converted back into an object\nhierarchy.  Pickling (and unpickling) is alternatively known as\n\u201cserialization\u201d, \u201cmarshalling,\u201d [1] or \u201cflattening\u201d; however, to avoid\nconfusion, the terms used here are \u201cpickling\u201d and \u201cunpickling\u201d.',
    "pickletools": 'This module contains various constants relating to the intimate\ndetails of the "pickle" module, some lengthy comments about the\nimplementation, and a few useful functions for analyzing pickled data.\nThe contents of this module are useful for Python core developers who\nare working on the "pickle"; ordinary users of the "pickle" module\nprobably won\u2019t find the "pickletools" module relevant.',
    "pipes": 'This module is no longer part of the Python standard library. It was\nremoved in Python 3.13 after being deprecated in Python 3.11.  The\nremoval was decided in **PEP 594**.\n\nApplications should use the "subprocess" module instead.\n\nThe last version of Python that provided the "pipes" module was Python\n3.12.',
    "pkgutil": "This module provides utilities for the import system, in particular\npackage support.",
    "platform": "Specific platforms listed alphabetically, with Linux included in the\nUnix section.",
    "plistlib": "This module provides an interface for reading and writing the\n\u201cproperty list\u201d files used by Apple, primarily on macOS and iOS. This\nmodule supports both binary and XML plist files.",
    "poplib": 'This module defines a class, "POP3", which encapsulates a connection\nto a POP3 server and implements the protocol as defined in **RFC\n1939**. The "POP3" class supports both the minimal and optional\ncommand sets from **RFC 1939**. The "POP3" class also supports the\n"STLS" command introduced in **RFC 2595** to enable encrypted\ncommunication on an already established connection.',
    "posix": "This module provides access to operating system functionality that is\nstandardized by the C Standard and the POSIX standard (a thinly\ndisguised Unix interface).",
    "pprint": 'The "pprint" module provides a capability to \u201cpretty-print\u201d arbitrary\nPython data structures in a form which can be used as input to the\ninterpreter. If the formatted structures include objects which are not\nfundamental Python types, the representation may not be loadable.\nThis may be the case if objects such as files, sockets or classes are\nincluded, as well as many other objects which are not representable as\nPython literals.',
    "profile": 'Python provides both *statistical profiling* and *deterministic\nprofiling* of Python programs. A *profile* is a set of statistics that\ndescribes how often and for how long various parts of the program\nexecuted. These statistics can be formatted into reports via the\n"pstats" module.',
    "pty": 'The "pty" module defines operations for handling the pseudo-terminal\nconcept: starting another process and being able to write to and read\nfrom its controlling terminal programmatically.',
    "pwd": "This module provides access to the Unix user account and password\ndatabase.  It is available on all Unix versions.",
    "py_compile": 'The "py_compile" module provides a function to generate a byte-code\nfile from a source file, and another function used when the module\nsource file is invoked as a script.',
    "pyclbr": 'The "pyclbr" module provides limited information about the functions,\nclasses, and methods defined in a Python-coded module.  The\ninformation is sufficient to implement a module browser.  The\ninformation is extracted from the Python source code rather than by\nimporting the module, so this module is safe to use with untrusted\ncode. This restriction makes it impossible to use this module with\nmodules not implemented in Python, including all standard and optional\nextension modules.',
    "pydoc": 'The "pydoc" module automatically generates documentation from Python\nmodules.  The documentation can be presented as pages of text on the\nconsole, served to a web browser, or saved to HTML files.',
    "pyexpat": "If you need to parse untrusted or unauthenticated data, see XML\nsecurity.",
    "queue": 'The "queue" module implements multi-producer, multi-consumer queues.\nIt is especially useful in threaded programming when information must\nbe exchanged safely between multiple threads.  The "Queue" class in\nthis module implements all the required locking semantics.',
    "quopri": 'This module performs quoted-printable transport encoding and decoding,\nas defined in **RFC 1521**: \u201cMIME (Multipurpose Internet Mail\nExtensions) Part One: Mechanisms for Specifying and Describing the\nFormat of Internet Message Bodies\u201d. The quoted-printable encoding is\ndesigned for data where there are relatively few nonprintable\ncharacters; the base64 encoding scheme available via the "base64"\nmodule is more compact if there are many such characters, as when\nsending a graphics file.',
    "random": "This module implements pseudo-random number generators for various\ndistributions.",
    "re": "This module provides regular expression matching operations similar to\nthose found in Perl.",
    "readline": 'The "readline" module defines a number of functions to facilitate\ncompletion and reading/writing of history files from the Python\ninterpreter. This module can be used directly, or via the\n"rlcompleter" module, which supports completion of Python identifiers\nat the interactive prompt.  Settings made using  this module affect\nthe behaviour of both the interpreter\u2019s interactive prompt  and the\nprompts offered by the built-in "input()" function.',
    "reprlib": 'The "reprlib" module provides a means for producing object\nrepresentations with limits on the size of the resulting strings. This\nis used in the Python debugger and may be useful in other contexts as\nwell.',
    "resource": "This module provides basic mechanisms for measuring and controlling\nsystem resources utilized by a program.",
    "rlcompleter": 'The "rlcompleter" module defines a completion function suitable to be\npassed to "set_completer()" in the "readline" module.',
    "runpy": 'The "runpy" module is used to locate and run Python modules without\nimporting them first. Its main use is to implement the "-m" command\nline switch that allows scripts to be located using the Python module\nnamespace rather than the filesystem.',
    "sched": 'The "sched" module defines a class which implements a general purpose\nevent scheduler:',
    "secrets": 'The "secrets" module is used for generating cryptographically strong\nrandom numbers suitable for managing data such as passwords, account\nauthentication, security tokens, and related secrets.',
    "security_warnings": "The following modules have specific security considerations:",
    "select": 'This module provides access to the "select()" and "poll()" functions\navailable in most operating systems, "devpoll()" available on Solaris\nand derivatives, "epoll()" available on Linux 2.5+ and "kqueue()"\navailable on most BSD. Note that on Windows, it only works for\nsockets; on other operating systems, it also works for other file\ntypes (in particular, on Unix, it works on pipes). It cannot be used\non regular files to determine whether a file has grown since it was\nlast read.',
    "selectors": 'This module allows high-level and efficient I/O multiplexing, built\nupon the "select" module primitives. Users are encouraged to use this\nmodule instead, unless they want precise control over the OS-level\nprimitives used.',
    "shelve": 'A \u201cshelf\u201d is a persistent, dictionary-like object.  The difference\nwith \u201cdbm\u201d databases is that the values (not the keys!) in a shelf can\nbe essentially arbitrary Python objects \u2014 anything that the "pickle"\nmodule can handle. This includes most class instances, recursive data\ntypes, and objects containing lots of shared  sub-objects.  The keys\nare ordinary strings.',
    "shlex": 'The "shlex" class makes it easy to write lexical analyzers for simple\nsyntaxes resembling that of the Unix shell.  This will often be useful\nfor writing minilanguages, (for example, in run control files for\nPython applications) or for parsing quoted strings.',
    "shutil": 'The "shutil" module offers a number of high-level operations on files\nand collections of files.  In particular, functions are provided\nwhich support file copying and removal. For operations on individual\nfiles, see also the "os" module.',
    "signal": "This module provides mechanisms to use signal handlers in Python.",
    "site": '**This module is automatically imported during initialization.** The\nautomatic import can be suppressed using the interpreter\u2019s "-S"\noption.',
    "smtpd": 'This module is no longer part of the Python standard library. It was\nremoved in Python 3.12 after being deprecated in Python 3.6.  The\nremoval was decided in **PEP 594**.\n\nA possible replacement is the third-party aiosmtpd library. This\nlibrary is not maintained or supported by the Python core team.\n\nThe last version of Python that provided the "smtpd" module was Python\n3.11.',
    "smtplib": 'The "smtplib" module defines an SMTP client session object that can be\nused to send mail to any internet machine with an SMTP or ESMTP\nlistener daemon.  For details of SMTP and ESMTP operation, consult\n**RFC 821** (Simple Mail Transfer Protocol) and **RFC 1869** (SMTP\nService Extensions).',
    "sndhdr": 'This module is no longer part of the Python standard library. It was\nremoved in Python 3.13 after being deprecated in Python 3.11.  The\nremoval was decided in **PEP 594**.\n\nPossible replacements are third-party modules from PyPI: filetype,\npuremagic, or python-magic. These are not supported or maintained by\nthe Python core team.\n\nThe last version of Python that provided the "sndhdr" module was\nPython 3.12.',
    "socket": "This module provides access to the BSD *socket* interface. It is\navailable on all modern Unix systems, Windows, MacOS, and probably\nadditional platforms.",
    "socketserver": 'The "socketserver" module simplifies the task of writing network\nservers.',
    "spwd": 'This module is no longer part of the Python standard library. It was\nremoved in Python 3.13 after being deprecated in Python 3.11.  The\nremoval was decided in **PEP 594**.\n\nA possible replacement is the third-party library python-pam. This\nlibrary is not supported or maintained by the Python core team.\n\nThe last version of Python that provided the "spwd" module was Python\n3.12.',
    "sqlite3": "SQLite is a C library that provides a lightweight disk-based database\nthat doesn\u2019t require a separate server process and allows accessing\nthe database using a nonstandard variant of the SQL query language.\nSome applications can use SQLite for internal data storage.  It\u2019s also\npossible to prototype an application using SQLite and then port the\ncode to a larger database such as PostgreSQL or Oracle.",
    "ssl": "This module provides access to Transport Layer Security (often known\nas \u201cSecure Sockets Layer\u201d) encryption and peer authentication\nfacilities for network sockets, both client-side and server-side.\nThis module uses the OpenSSL library. It is available on all modern\nUnix systems, Windows, macOS, and probably additional platforms, as\nlong as OpenSSL is installed on that platform.",
    "stat": 'The "stat" module defines constants and functions for interpreting the\nresults of "os.stat()", "os.fstat()" and "os.lstat()" (if they exist).\nFor complete details about the "stat()", "fstat()" and "lstat()"\ncalls, consult the documentation for your system.',
    "statistics": 'This module provides functions for calculating mathematical statistics\nof numeric ("Real"-valued) data.',
    "stdtypes": "The following sections describe the standard types that are built into\nthe interpreter.",
    "string": "Text Sequence Type \u2014 str",
    "string.templatelib": "Format strings",
    "stringprep": "When identifying things (such as host names) in the internet, it is\noften necessary to compare such identifications for \u201cequality\u201d.\nExactly how this comparison is executed may depend on the application\ndomain, e.g. whether it should be case-insensitive or not. It may be\nalso necessary to restrict the possible identifications, to allow only\nidentifications consisting of \u201cprintable\u201d characters.",
    "struct": 'This module converts between Python values and C structs represented\nas Python "bytes" objects.  Compact format strings describe the\nintended conversions to/from Python values. The module\u2019s functions and\nobjects can be used for two largely distinct applications, data\nexchange with external sources (files or network connections), or data\ntransfer between the Python application and the C layer.',
    "subprocess": 'The "subprocess" module allows you to spawn new processes, connect to\ntheir input/output/error pipes, and obtain their return codes.  This\nmodule intends to replace several older modules and functions:',
    "sunau": 'This module is no longer part of the Python standard library. It was\nremoved in Python 3.13 after being deprecated in Python 3.11.  The\nremoval was decided in **PEP 594**.\n\nThe last version of Python that provided the "sunau" module was Python\n3.12.',
    "symtable": 'Symbol tables are generated by the compiler from AST just before\nbytecode is generated.  The symbol table is responsible for\ncalculating the scope of every identifier in the code.  "symtable"\nprovides an interface to examine these tables.',
    "sys": "This module provides access to some variables used or maintained by\nthe interpreter and to functions that interact strongly with the\ninterpreter. It is always available. Unless explicitly noted\notherwise, all variables are read-only.",
    "sys.monitoring": '"sys.monitoring" is a namespace within the "sys" module, not an\nindependent module, so there is no need to "import sys.monitoring",\nsimply "import sys" and then use "sys.monitoring".',
    "sys_path_init": 'A module search path is initialized when Python starts. This module\nsearch path may be accessed at "sys.path".',
    "sysconfig": 'The "sysconfig" module provides access to Python\u2019s configuration\ninformation like the list of installation paths and the configuration\nvariables relevant for the current platform.',
    "syslog": 'This module provides an interface to the Unix "syslog" library\nroutines. Refer to the Unix manual pages for a detailed description of\nthe "syslog" facility.',
    "tabnanny": 'For the time being this module is intended to be called as a script.\nHowever it is possible to import it into an IDE and use the function\n"check()" described below.',
    "tarfile": 'The "tarfile" module makes it possible to read and write tar archives,\nincluding those using gzip, bz2 and lzma compression. Use the\n"zipfile" module to read or write ".zip" files, or the higher-level\nfunctions in shutil.',
    "telnetlib": 'This module is no longer part of the Python standard library. It was\nremoved in Python 3.13 after being deprecated in Python 3.11.  The\nremoval was decided in **PEP 594**.\n\nPossible replacements are third-party libraries from PyPI: telnetlib3\nor Exscript.  These are not supported or maintained by the Python core\nteam.\n\nThe last version of Python that provided the "telnetlib" module was\nPython 3.12.',
    "tempfile": 'This module creates temporary files and directories.  It works on all\nsupported platforms. "TemporaryFile", "NamedTemporaryFile",\n"TemporaryDirectory", and "SpooledTemporaryFile" are high-level\ninterfaces which provide automatic cleanup and can be used as *context\nmanagers*. "mkstemp()" and "mkdtemp()" are lower-level functions which\nrequire manual cleanup.',
    "termios": "This module provides an interface to the POSIX calls for tty I/O\ncontrol. For a complete description of these calls, see *termios(3)*\nUnix manual page.  It is only available for those Unix versions that\nsupport POSIX *termios* style tty I/O control configured during\ninstallation.",
    "test": 'The "test" package is meant for internal use by Python only. It is\ndocumented for the benefit of the core developers of Python. Any use\nof this package outside of Python\u2019s standard library is discouraged as\ncode mentioned here can change or be removed without notice between\nreleases of Python.',
    "textwrap": 'The "textwrap" module provides some convenience functions, as well as\n"TextWrapper", the class that does all the work. If you\u2019re just\nwrapping or filling one or two text strings, the convenience functions\nshould be good enough; otherwise, you should use an instance of\n"TextWrapper" for efficiency.',
    "threading": 'This module constructs higher-level threading interfaces on top of the\nlower level "_thread" module.',
    "time": 'This module provides various time-related functions. For related\nfunctionality, see also the "datetime" and "calendar" modules.',
    "timeit": "This module provides a simple way to time small bits of Python code.\nIt has both a Command-Line Interface as well as a callable one.  It\navoids a number of common traps for measuring execution times. See\nalso Tim Peters\u2019 introduction to the \u201cAlgorithms\u201d chapter in the\nsecond edition of *Python Cookbook*, published by O\u2019Reilly.",
    "tk": 'Tk/Tcl has long been an integral part of Python.  It provides a robust\nand platform independent windowing toolkit, that is available to\nPython programmers using the "tkinter" package, and its extension, the\n"tkinter.ttk" module.',
    "tkinter": 'The "tkinter" package (\u201cTk interface\u201d) is the standard Python\ninterface to the Tcl/Tk GUI toolkit.  Both Tk and "tkinter" are\navailable on most Unix platforms, including macOS, as well as on\nWindows systems.',
    "tkinter.colorchooser": 'The "tkinter.colorchooser" module provides the "Chooser" class as an\ninterface to the native color picker dialog. "Chooser" implements a\nmodal color choosing dialog window. The "Chooser" class inherits from\nthe "Dialog" class.',
    "tkinter.dnd": "This is experimental and due to be deprecated when it is replaced with\nthe Tk DND.",
    "tkinter.font": 'The "tkinter.font" module provides the "Font" class for creating and\nusing named fonts.',
    "tkinter.messagebox": 'The "tkinter.messagebox" module provides a template base class as well\nas a variety of convenience methods for commonly used configurations.\nThe message boxes are modal and will return a subset of ("True",\n"False", "None", "OK", "CANCEL", "YES", "NO") based on the user\u2019s\nselection. Common message box styles and layouts include but are not\nlimited to:',
    "tkinter.scrolledtext": 'The "tkinter.scrolledtext" module provides a class of the same name\nwhich implements a basic text widget which has a vertical scroll bar\nconfigured to do the \u201cright thing.\u201d  Using the "ScrolledText" class is\na lot easier than setting up a text widget and scroll bar directly.',
    "tkinter.ttk": 'The "tkinter.ttk" module provides access to the Tk themed widget set,\nintroduced in Tk 8.5. It provides additional benefits including anti-\naliased font rendering under X11 and window transparency (requiring a\ncomposition window manager on X11).',
    "token": 'This module provides constants which represent the numeric values of\nleaf nodes of the parse tree (terminal tokens).  Refer to the file\n"Grammar/Tokens" in the Python distribution for the definitions of the\nnames in the context of the language grammar.  The specific numeric\nvalues which the names map to may change between Python versions.',
    "tokenize": 'The "tokenize" module provides a lexical scanner for Python source\ncode, implemented in Python.  The scanner in this module returns\ncomments as tokens as well, making it useful for implementing \u201cpretty-\nprinters\u201d, including colorizers for on-screen displays.',
    "tomllib": "This module provides an interface for parsing TOML 1.0.0 (Tom\u2019s\nObvious Minimal Language, https://toml.io). This module does not\nsupport writing TOML.",
    "trace": 'The "trace" module allows you to trace program execution, generate\nannotated statement coverage listings, print caller/callee\nrelationships and list functions executed during a program run.  It\ncan be used in another program or from the command line.',
    "traceback": "This module provides a standard interface to extract, format and print\nstack traces of Python programs. It is more flexible than the\ninterpreter\u2019s default traceback display, and therefore makes it\npossible to configure certain aspects of the output. Finally, it\ncontains a utility for capturing enough information about an exception\nto print it later, without the need to save a reference to the actual\nexception. Since exceptions can be the roots of large objects graph,\nthis utility can significantly improve memory management.",
    "tracemalloc": "The tracemalloc module is a debug tool to trace memory blocks\nallocated by Python. It provides the following information:",
    "tty": 'The "tty" module defines functions for putting the tty into cbreak and\nraw modes.',
    "turtle": "Turtle graphics is an implementation of the popular geometric drawing\ntools introduced in Logo, developed by Wally Feurzeig, Seymour Papert\nand Cynthia Solomon in 1967.",
    "types": "This module defines utility functions to assist in dynamic creation of\nnew types.",
    "typing": "The Python runtime does not enforce function and variable type\nannotations. They can be used by third party tools such as *type\ncheckers*, IDEs, linters, etc.",
    "unicodedata": "This module provides access to the Unicode Character Database (UCD)\nwhich defines character properties for all Unicode characters. The\ndata contained in this database is compiled from the UCD version\n16.0.0.",
    "unittest": "(If you are already familiar with the basic concepts of testing, you\nmight want to skip to the list of assert methods.)",
    "unittest.mock": '"unittest.mock" is a library for testing in Python. It allows you to\nreplace parts of your system under test with mock objects and make\nassertions about how they have been used.',
    "unittest.mock-examples": 'Common uses for "Mock" objects include:',
    "urllib": '"urllib" is a package that collects several modules for working with\nURLs:',
    "urllib.error": 'The "urllib.error" module defines the exception classes for exceptions\nraised by "urllib.request".  The base exception class is "URLError".',
    "urllib.parse": "This module defines a standard interface to break Uniform Resource\nLocator (URL) strings up in components (addressing scheme, network\nlocation, path etc.), to combine the components back into a URL\nstring, and to convert a \u201crelative URL\u201d to an absolute URL given a\n\u201cbase URL.\u201d",
    "urllib.request": 'The "urllib.request" module defines functions and classes which help\nin opening URLs (mostly HTTP) in a complex world \u2014 basic and digest\nauthentication, redirections, cookies and more.',
    "urllib.robotparser": 'This module provides a single class, "RobotFileParser", which answers\nquestions about whether or not a particular user agent can fetch a URL\non the web site that published the "robots.txt" file.  For more\ndetails on the structure of "robots.txt" files, see\nhttp://www.robotstxt.org/orig.html.',
    "uu": 'This module is no longer part of the Python standard library. It was\nremoved in Python 3.13 after being deprecated in Python 3.11.  The\nremoval was decided in **PEP 594**.\n\nThe last version of Python that provided the "uu" module was Python\n3.12.',
    "uuid": 'This module provides immutable "UUID" objects (the "UUID" class) and\nfunctions for generating UUIDs corresponding to a specific UUID\nversion as specified in **RFC 9562** (which supersedes **RFC 4122**),\nfor example, "uuid1()" for UUID version 1, "uuid3()" for UUID version\n3, and so on. Note that UUID version 2 is deliberately omitted as it\nis outside the scope of the RFC.',
    "venv": 'The "venv" module supports creating lightweight \u201cvirtual\nenvironments\u201d, each with their own independent set of Python packages\ninstalled in their "site" directories. A virtual environment is\ncreated on top of an existing Python installation, known as the\nvirtual environment\u2019s \u201cbase\u201d Python, and by default is isolated from\nthe packages in the base environment, so that only those explicitly\ninstalled in the virtual environment are available. See Virtual\nEnvironments and "site"\u2019s virtual environments documentation for more\ninformation.',
    "warnings": "Warning messages are typically issued in situations where it is useful\nto alert the user of some condition in a program, where that condition\n(normally) doesn\u2019t warrant raising an exception and terminating the\nprogram.  For example, one might want to issue a warning when a\nprogram uses an obsolete module.",
    "wave": 'The "wave" module provides a convenient interface to the Waveform\nAudio \u201cWAVE\u201d (or \u201cWAV\u201d) file format. Only uncompressed PCM encoded\nwave files are supported.',
    "weakref": 'The "weakref" module allows the Python programmer to create *weak\nreferences* to objects.',
    "webbrowser": 'The "webbrowser" module provides a high-level interface to allow\ndisplaying web-based documents to users. Under most circumstances,\nsimply calling the "open()" function from this module will do the\nright thing.',
    "windows": "This chapter describes modules that are only available on MS Windows\nplatforms.",
    "winreg": "These functions expose the Windows registry API to Python.  Instead of\nusing an integer as the registry handle, a handle object is used to\nensure that the handles are closed correctly, even if the programmer\nneglects to explicitly close them.",
    "winsound": 'The "winsound" module provides access to the basic sound-playing\nmachinery provided by Windows platforms.  It includes functions and\nseveral constants.',
    "wsgiref": "The Web Server Gateway Interface (WSGI) is a standard interface\nbetween web server software and web applications written in Python.\nHaving a standard interface makes it easy to use an application that\nsupports WSGI with a number of different web servers.",
    "xdrlib": 'This module is no longer part of the Python standard library. It was\nremoved in Python 3.13 after being deprecated in Python 3.11.  The\nremoval was decided in **PEP 594**.\n\nThe last version of Python that provided the "xdrlib" module was\nPython 3.12.',
    "xml": 'Python\u2019s interfaces for processing XML are grouped in the "xml"\npackage.',
    "xml.dom": "The Document Object Model, or \u201cDOM,\u201d is a cross-language API from the\nWorld Wide Web Consortium (W3C) for accessing and modifying XML\ndocuments.  A DOM implementation presents an XML document as a tree\nstructure, or allows client code to build such a structure from\nscratch.  It then gives access to the structure through a set of\nobjects which provided well-known interfaces.",
    "xml.dom.minidom": '"xml.dom.minidom" is a minimal implementation of the Document Object\nModel interface, with an API similar to that in other languages.  It\nis intended to be simpler than the full DOM and also significantly\nsmaller.  Users who are not already proficient with the DOM should\nconsider using the "xml.etree.ElementTree" module for their XML\nprocessing instead.',
    "xml.dom.pulldom": 'The "xml.dom.pulldom" module provides a \u201cpull parser\u201d which can also\nbe asked to produce DOM-accessible fragments of the document where\nnecessary. The basic concept involves pulling \u201cevents\u201d from a stream\nof incoming XML and processing them. In contrast to SAX which also\nemploys an event-driven processing model together with callbacks, the\nuser of a pull parser is responsible for explicitly pulling events\nfrom the stream, looping over those events until either processing is\nfinished or an error condition occurs.',
    "xml.etree.elementtree": 'The "xml.etree.ElementTree" module implements a simple and efficient\nAPI for parsing and creating XML data.',
    "xml.sax": 'The "xml.sax" package provides a number of modules which implement the\nSimple API for XML (SAX) interface for Python.  The package itself\nprovides the SAX exceptions and the convenience functions which will\nbe most used by users of the SAX API.',
    "xml.sax.handler": 'The SAX API defines five kinds of handlers: content handlers, DTD\nhandlers, error handlers, entity resolvers and lexical handlers.\nApplications normally only need to implement those interfaces whose\nevents they are interested in; they can implement the interfaces in a\nsingle object or in multiple objects. Handler implementations should\ninherit from the base classes provided in the module\n"xml.sax.handler", so that all methods get default implementations.',
    "xml.sax.reader": 'SAX parsers implement the "XMLReader" interface. They are implemented\nin a Python module, which must provide a function "create_parser()".\nThis function is invoked by  "xml.sax.make_parser()" with no arguments\nto create a new  parser object.',
    "xml.sax.utils": 'The module "xml.sax.saxutils" contains a number of classes and\nfunctions that are commonly useful when creating SAX applications,\neither in direct use, or as base classes.',
    "xmlrpc": "XML-RPC is a Remote Procedure Call method that uses XML passed via\nHTTP as a transport.  With it, a client can call methods with\nparameters on a remote server (the server is named by a URI) and get\nback structured data.",
    "xmlrpc.client": "XML-RPC is a Remote Procedure Call method that uses XML passed via\nHTTP(S) as a transport.  With it, a client can call methods with\nparameters on a remote server (the server is named by a URI) and get\nback structured data.  This module supports writing XML-RPC client\ncode; it handles all the details of translating between conformable\nPython objects and XML on the wire.",
    "xmlrpc.server": 'The "xmlrpc.server" module provides a basic server framework for XML-\nRPC servers written in Python.  Servers can either be free standing,\nusing "SimpleXMLRPCServer", or embedded in a CGI environment, using\n"CGIXMLRPCRequestHandler".',
    "zipapp": "This module provides tools to manage the creation of zip files\ncontaining Python code, which can be  executed directly by the Python\ninterpreter.  The module provides both a Command-Line Interface and a\nPython API.",
    "zipfile": "The ZIP file format is a common archive and compression standard. This\nmodule provides tools to create, read, write, append, and list a ZIP\nfile.  Any advanced use of this module will require an understanding\nof the format, as defined in PKZIP Application Note.",
    "zipimport": 'This module adds the ability to import Python modules ("*.py",\n"*.pyc") and packages from ZIP-format archives. It is usually not\nneeded to use the "zipimport" module explicitly; it is automatically\nused by the built-in "import" mechanism for "sys.path" items that are\npaths to ZIP archives.',
    "zlib": "For applications that require data compression, the functions in this\nmodule allow compression and decompression, using the zlib library.\nThe zlib library has its own home page at https://www.zlib.net.\nThere are known incompatibilities between the Python module and\nversions of the zlib library earlier than 1.1.3; 1.1.3 has a security\nvulnerability, so we recommend using 1.1.4 or later.",
    "zoneinfo": 'The "zoneinfo" module provides a concrete time zone implementation to\nsupport the IANA time zone database as originally specified in **PEP\n615**. By default, "zoneinfo" uses the system\u2019s time zone data if\navailable; if no system time zone data is available, the library will\nfall back to using the first-party tzdata package available on PyPI.',
}
